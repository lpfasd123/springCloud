server:
  port: 7905
eureka:
  client:
    healthcheck:
        enabled: true
    service-url:
      defaultZone: http://user:password123@localhost:8761/eureka
  #false注册名或者true注册ip
  instance:
    prefer-ip-address: true
spring:
  application:
    name: movie-ribbon-foo-2
